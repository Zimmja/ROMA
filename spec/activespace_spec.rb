# frozen_string_literal: true

require 'activespace'

describe ActiveSpace do
  # describe '.new_user' do
  #   it 'Sets the ActiveUser with new values' do
  #     ActiveSpace.set_space("Love Shack", "3", "1", "B-52s")
  #     expect(ActiveSpace.name).to eq "Love Shack"
  #     expect(ActiveSpace.bedrooms).to eq "3"
  #     expect(ActiveSpace.id).to eq "1"
  #     expect(ActiveSpace.hostname).to eq "B-52s"
  #   end
  # end
end